namespace DiscordScraper.Contracts.IR;

public enum MentionKind { User, Role, Everyone, Here }
public enum EmojiKind { Unicode, Custom }
public enum TimestampStyle { ShortTime, LongTime, ShortDate, LongDate, ShortDateTime, LongDateTime, Relative }
public enum FormattingKind { Bold, Italic, Underline, Strikethrough, Spoiler }
